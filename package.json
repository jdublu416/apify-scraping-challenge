{
	"name": "apify_scraper_challenge",
	"version": "1.0.0",
	"description": "Crawl a number of web pages using headless Chrome and Puppeteer",
	"main": "main.js",
	"dependencies": {
		"apify": "^0.15.2",
		"util": "^0.11.1"
	},
	"devDependencies": {},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"clean": "rm -r apify_storage && mkdir apify_storage",
		"start": "node main.js",
		"dev": "npm run clean && node main.js"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/jdublu416/apify-scraping-challenge"
	},
	"author": "Janet Warren",
	"license": "ISC"
}
